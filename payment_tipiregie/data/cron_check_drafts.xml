<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">
        <!-- Cron to check PayFIP draft payment transactions -->
        <record id="cron_check_draft_payment_transactions" model="ir.cron">
            <field name="name">Cron to check PayFIP draft payment transactions</field>
            <field name="user_id" ref="base.user_root"/>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="numbercall">-1</field>
            <field name="nextcall"
                   eval="(datetime.today()+timedelta(days=1)).replace(hour=2,minute=0,second=0).strftime('%Y-%m-%d %H:%M:%S')"/>
            <field name="doall" eval="False"/>
            <field name="args" eval="({'number_of_days':1,'send_summary':False},)"/>
            <field name="model" eval="'payment.transaction'"/>
            <field name="function" eval="'tipiregie_cron_check_draft_payment_transactions'"/>
            <field name="active" eval="True"/>
        </record>
    </data>
</odoo>
